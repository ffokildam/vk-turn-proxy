services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.server
    container_name: vk-turn-server
    ports:
      - "56000:56000/udp"
    command: ["/main", "-listen", "0.0.0.0:56000", "-connect", "127.0.0.1:${LOCAL_WG_PORT}"]
